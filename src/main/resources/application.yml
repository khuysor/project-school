spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project_web
    username: "postgres"
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect